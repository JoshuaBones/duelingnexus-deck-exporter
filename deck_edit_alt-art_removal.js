(function () {
  function removeAltArt() {
    Editor.updateDeck();

    const Deck = window.Deck;
    if (!Deck) return;

    const deck = window.Deck;
    //Editor.database.cards[#]
    

    Editor.updateDeck();
  }
})();